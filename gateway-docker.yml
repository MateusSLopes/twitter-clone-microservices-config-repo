eureka:
  host: localhost
  name: eureka
  port: 8081

spring:
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: users-ms
              uri: lb://users-ms
              predicates:
                - Path=/users,/users/**
              
  

            
